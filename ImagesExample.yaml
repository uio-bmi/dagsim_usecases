graph:
  nodes:
    U1: uniform(0,1)
    U2: uniform(0,1)
    H: binomial(1, U1)
    C: binomial(1, U2)
    V: Ber(U1)
    R: BerExp(C, H, "H")
    Y: BerExp(C, V, "V")
    Image: drawImage(H, V, R, C)
  python_file: ImagesExample.py

instructions:
  simulation:
    csv_name: Images_metadata
    num_samples: 50
    output_path: "./images"
