graph:
  nodes:
    U1: uniform(0, 1)
    U2: uniform
    H: binomial(1, U1)
    C: binomial(1, U2)
    V: Ber(U1)
    R: BerExp(C, H, "H")
    Y: BerExp(C, V, "V")
    I: generateImage(H, V, R, C)
  python_file: ImagesExample_v1.py

instructions:
  simulation:
    csv_name: Images_metadata
    num_samples: 50
    output_path: "./images"
